import React, { useState, useEffect } from 'react'; import axios from 'axios'; const ProfilePage = () => { const [user, setUser] = useState({}); const [error, setError] = useState(null); useEffect(() => { axios.get('/api/profile/') .then(response => { setUser(response.data); }) .catch(error => { setError(error.message); }); }, []); const handleUpdate = (event) => { event.preventDefault(); axios.put('/api/profile/', user) .then(response => { setUser(response.data); }) .catch(error => { setError(error.message); }); }; return ( <div> <h1>Profile Page</h1> <form onSubmit={handleUpdate}> <label>Username:</label> <input type="text" value={user.username} onChange={(event) => setUser({ ...user, username: event.target.value })} /> <br /> <label>Email:</label> <input type="email" value={user.email} onChange={(event) => setUser({ ...user, email: event.target.value })} /> <br /> <button type="submit">Update</button> </form> {error && <p style={{ color: 'red' }}>{error}</p>} </div> ); }; export default ProfilePage;